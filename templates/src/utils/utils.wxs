/* eslint-disable */
var includes = function (data, value) {
	return data.indexOf(value) !== -1;
}; 

// 模版里会报错
var parseInt = function (value, number) {
	return parseInt(value, number || 10);
}; 

module.exports = {
	includes: includes,
	parseInt: parseInt
};